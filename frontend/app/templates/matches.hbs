<div class="row">
  <div class="medium-4 columns">
    <ul class="side-nav">
      {{#each}}
        <li>{{#link-to 'matches.match' id}}{{peer.name}}{{/link-to}}</li>
      {{/each}}
    </ul>
  </div>
  <div class="medium-8 columns">
    {{outlet}}
  </div>
</div>